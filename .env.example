# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
# For development: Use Replit Database URL (automatically provided)
# For Vercel production: Use Vercel Postgres, Neon, or Supabase pooled connection
DATABASE_URL=postgresql://user:password@host:port/database

# Supabase Configuration (Optional)
# Get these from https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Session Configuration
# IMPORTANT: Generate a secure random string (at least 32 characters)
# Use: https://generate-random.org/api-key-generator
SESSION_SECRET=generate-a-secure-random-string-here

# CORS Configuration (comma-separated for multiple origins)
# For development: http://localhost:5000
# For Vercel: Add your Vercel app URL (e.g., https://your-app.vercel.app)
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
AUTH_RATE_LIMIT_MAX=10

# Security
BCRYPT_SALT_ROUNDS=12

# Feature Flags
ENABLE_WEBSOCKET=true
ENABLE_GUEST_MODE=true

# Logging
LOG_LEVEL=info

# ============================================
# VERCEL DEPLOYMENT VARIABLES
# ============================================
# Set these in Vercel Dashboard → Your Project → Settings → Environment Variables
#
# Required for Production:
# - NODE_ENV=production
# - DATABASE_URL (from Vercel Postgres, Neon, or Supabase)
# - SESSION_SECRET (generate a secure random string)
# - ALLOWED_ORIGINS (your Vercel app URL)
#
# Optional (if using these features):
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - STRIPE_SECRET_KEY (if using Stripe)
# - VITE_STRIPE_PUBLISHABLE_KEY (if using Stripe)
